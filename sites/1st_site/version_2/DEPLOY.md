# 🚀 Деплой на GitHub Pages

## ✅ Что уже настроено:

1. ✅ `vite.config.js` - добавлен `base: '/Work/1st_site/version_2/'`
2. ✅ `package.json` - добавлены скрипты `predeploy` и `deploy`
3. ✅ `gh-pages` - уже установлен в devDependencies
4. ✅ `homepage` - указан в package.json

## 📋 Пошаговая инструкция:

### 1️⃣ Убедитесь, что gh-pages установлен

```bash
npm install --save-dev gh-pages
```

(Уже установлен, но можно проверить)

### 2️⃣ Задеплойте сайт

```bash
npm run deploy
```

**Что произойдет:**
- Автоматически выполнится `npm run build` (создаст `dist/`)
- Скрипт `prepare-deploy.js` создаст структуру `deploy-dist/1st_site/version_2/`
- Файлы из `dist/` будут скопированы в `deploy-dist/1st_site/version_2/`
- `gh-pages` создаст ветку `gh-pages` в вашем репозитории
- Файлы из `deploy-dist/` загрузятся в ветку `gh-pages`

### 3️⃣ Включите GitHub Pages (один раз)

1. Перейдите на GitHub: `https://github.com/legendacr/Work`
2. Откройте **Settings** → **Pages**
3. В разделе **Source** выберите:
   - **Deploy from a branch**
   - **Branch:** `gh-pages`
   - **Folder:** `/ (root)`
4. Нажмите **Save**

### 4️⃣ Готово! 🎉

Через 1-2 минуты сайт будет доступен по адресу:

**https://legendacr.github.io/Work/1st_site/version_2/**

---

## 🔄 Обновление сайта

После любых изменений в коде:

```bash
npm run deploy
```

Сайт автоматически обновится через 1-2 минуты.

---

## ⚠️ Важные замечания:

1. **Путь в `vite.config.js`** должен точно совпадать с путем на GitHub
2. **Все изменения** нужно сначала закоммитить в основную ветку
3. **`npm run deploy`** работает только если у вас есть доступ к репозиторию

---

## 🐛 Решение проблем:

### Белый экран на GitHub Pages?
- Проверьте, что `base` в `vite.config.js` правильный
- Убедитесь, что путь начинается и заканчивается на `/`

### Ошибка при деплое?
- Проверьте, что вы авторизованы в Git: `git config --global user.name`
- Убедитесь, что репозиторий существует и у вас есть права

### Сайт не обновляется?
- Подождите 2-3 минуты (GitHub Pages кеширует)
- Очистите кеш браузера (Ctrl+Shift+R)
